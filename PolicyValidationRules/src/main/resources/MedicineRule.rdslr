import redhat.policyvalidationrules.model.RapidRequest;
import redhat.policyvalidationrules.model.Medicine;

rule "MedicineRule"
	dialect "mvel"
	when
		If there is a Rapid Policy Request with  Medication of color  "Red"
	then
		Setting "Conditional"
end
